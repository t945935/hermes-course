# H2 實作紀錄：安裝與健康檢查

> 完成日期：2026-09-14 ｜ 環境：WSL2 Ubuntu 24.04

## 安裝狀態
- Hermes Agent v0.21.2 (2026.9.11)，安裝方式：git installer（`~/.hermes/hermes-agent`）
- 安裝腳本自動處理了：uv、Python 3.11.15、Node.js、Playwright Chromium、指令 symlink

## hermes doctor 結果重點（✓ / ⚠）
| 項目 | 狀態 | 說明 |
|---|---|---|
| Security Advisories | ✓ | 無安全公告 |
| Python 3.11.15 + venv | ✓ | 版本一致 |
| SSL 憑證 | ✓ | 有效 |
| 必要套件（OpenAI SDK/Rich/dotenv…） | ✓ | 齊全 |
| config / .env | ✓ | 存在（版本為 v0 → 待遷移，已用 --fix 修復 1 項） |
| 目錄結構 | ✓ | sessions/memories/skills/cron/logs 俱在 |
| 指令安裝 | ✓ | venv 與 ~/.local/bin 都正確 |
| 外部工具 | ⚠ | git ✓｜ripgrep 缺（grep 替代）｜docker 缺（選用）｜Node ✓ |
| API 連線 | ⚠ | 40 項並行檢查，OpenRouter 未設定（正常，未選用） |
| 工具可用性 | ✓ 15 項 | browser/code_execution/delegation/file/terminal/memory 等 |
| 待設定工具 | ⚠ | discord/image_gen/vision/x_search 等——皆因缺 API key（此為預期） |

## 已執行修復
- `hermes doctor --fix` → 修復 1 項（config 遷移），剩 3 項需手動：
  1. agent-browser npm 漏洞（2 個，非 critical）
  2. web workspace npm 漏洞（6 個，build-time 工具，無 runtime 風險）
  3. 缺 API keys（`hermes setup` 解決，見 H3）

## 驗收自評
- ✅ doctor 無 ✗ 級錯誤
- ✅ 能解釋 2 個 ⚠：
  - ripgrep 未裝 → 檔案搜尋較慢，`sudo apt install ripgrep` 可修
  - 工具 ⚠ 多因缺 API key → 屬預期狀態，H3 setup 後消失